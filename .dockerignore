# The frontend image runs `npm ci` fresh, so none of these belong in the build
# context — sending them to the Docker daemon just slows every `make deploy`.
node_modules
dist
dist-ssr
.git
.github
src-tauri/target
src-tauri/gen

# Local/editor/env cruft
*.log
.env
.env.*
.vscode
.idea
.DS_Store
data
